{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "eas-build-pre-install": "npm --prefix artifacts/yatra-guard install",
    "postinstall": "npm --prefix artifacts/yatra-guard install"
  }
}
